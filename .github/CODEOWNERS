# Who owns what, for the paths where a change means a change to the rules.
#
# The organisation has no teams, so owners are individuals. Four accounts hold admin on this
# repository: @RamzesX, @filip-plata, @jakubs21, @pZmudzki.
#
# Today this file documents ownership rather than enforcing it: the ruleset on main requires a
# pull request and a green `Merge verdict`, but zero approvals, because GitHub does not let an
# author approve their own pull request and this repository has one maintainer. Raising that count
# to 1 the moment a second person has write access turns every line below into a hard gate with no
# other change needed.

*                                   @RamzesX

# ── The invariant surfaces ────────────────────────────────────────────────────

# Ratchets and budgets.
/tools/ci/fuzz-baseline.json        @RamzesX
/docs/testing/measured-counts.json  @RamzesX

# The gates.
/.github/workflows/                 @RamzesX
/tools/ci/fuzz-verdict.mjs          @RamzesX
/tools/ci/pit-summary.mjs           @RamzesX
/tools/ci/measure-counts.mjs        @RamzesX

# The contract, producer side: openapi.json is generated by a test, never edited by hand.
/docs/openapi/                      @RamzesX

# Scanner configuration, and the written dispositions that justify every dismissal.
/sonar-project.properties           @RamzesX
/owasp-suppressions.xml             @RamzesX
/spotbugs-security-exclude.xml      @RamzesX
/spotbugs-security-include.xml      @RamzesX
/pmd-ruleset.xml                    @RamzesX
/docs/security/                     @RamzesX

# The money state machine: the domain the worked example is about.
/src/main/java/com/sm/instagram/platform/subscription/  @RamzesX
