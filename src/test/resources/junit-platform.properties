# JUnit 5 Platform Configuration for Sequential Test Execution
# This ensures tests run sequentially to prevent conflicts

# Disable parallel execution completely
junit.jupiter.execution.parallel.enabled=false
junit.jupiter.execution.parallel.mode.default=same_thread
junit.jupiter.execution.parallel.mode.classes.default=same_thread

# Use per-method lifecycle to ensure clean state for each test
# With @Transactional, each test gets rolled back automatically
junit.jupiter.testinstance.lifecycle.default=per_method

# Test execution order
junit.jupiter.testmethod.order.default=org.junit.jupiter.api.MethodOrderer$OrderAnnotation

# Display names
junit.jupiter.displayname.generator.default=org.junit.jupiter.api.DisplayNameGenerator$ReplaceUnderscores

# Timeout configuration - generous timeout for integration tests
junit.jupiter.execution.timeout.default=60s
junit.jupiter.execution.timeout.mode=disabled_on_debug

# Conditional test execution
junit.jupiter.conditions.deactivate=org.junit.*DisabledCondition
