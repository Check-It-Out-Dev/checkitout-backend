# Handlers for 04-docker role

---
- name: Restart Docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
  listen: "Restart Docker"

- name: Reload Docker
  ansible.builtin.systemd:
    name: docker
    state: reloaded
  listen: "Reload Docker"
