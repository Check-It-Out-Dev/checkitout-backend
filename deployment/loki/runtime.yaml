# Runtime configuration for Loki
# This file can be used for dynamic configuration updates
# Loki will reload this file based on the period specified in the main config

# Empty for now - add overrides as needed
overrides: {}
