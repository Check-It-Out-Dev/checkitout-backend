# =============================================================================
# mTLS Configuration for Loki (Simplified)
# /etc/nginx/conf.d/mtls-loki.conf
# 
# This file is empty as all mTLS settings are now directly in the site config
# to avoid variable scope issues.
# 
# The ssl_client_certificate, ssl_verify_client, and ssl_verify_depth
# directives are set directly in the server block of loki.checkitout.app.conf
# =============================================================================

# This file intentionally left minimal to avoid conflicts
# All mTLS configuration is in the site-specific configuration